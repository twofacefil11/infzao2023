#include <iostream>
#include <time.h>

using namespace std;

int main(void) {
    srand(time(NULL));

    long p;

    cout << "Podaj liczbe punkuf: ";
    cin >> p;
    cout << endl;

    for (int i = 0; i < )

    return 0;
}