#include <iostream>
#include <time.h>

using namespace std;

int main(void) {
    srand(time(NULL));

    long p;

    cout << "Podaj liczbe punkuf: ";
    cin >> p;


    for (int i = 0; i < )

    return 0;
}